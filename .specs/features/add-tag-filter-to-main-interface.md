---
id: add-tag-filter-to-main-interface
title: Add tag filter to main interface
status: draft
milestone: browsing
priority: medium
handoff: single
---

## Overview

Include tag filtering facet in the left navigation filters panel to allow users to filter results by tags they've applied

## Requirements

- Resolves flow analysis finding: "Tag filtering capability missing from main interface filters"
- User journey step 9 shows users adding tags via image detail view, but the main-interface-screen left navigation filters panel doesn't specify tag filtering capability. Users can add tags but cannot filter by them in the primary interface, breaking the tag workflow usefulness.

## Acceptance Criteria

- [ ] Gap identified by flow analysis is resolved

## Tasks

- [ ] Implement feature | backend
